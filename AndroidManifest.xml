<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.goeuro.matheszabi.assignment"
    android:versionCode="1"
    android:versionName="1.0" xmlns:tools="http://schemas.android.com/tools">

    <uses-sdk
        android:minSdkVersion="19"
        android:targetSdkVersion="19" tools:ignore="OldTargetApi"/>

    <!-- To access Internet: -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!--  If you are drunk and you don't know where you are maybe the Android is better on this -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        
        android:theme="@style/GangnamStyle" >
        <!-- A clean, well-animated, beautiful UI is very important.
        ########################################
         Please, let your imagination fly here. 
        ########################################
       	style: "GangnamStyle", take it! :) 
         -->
        <activity
            android:name=".FunnyActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
